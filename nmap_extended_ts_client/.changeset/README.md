# Changesets

This folder controls semver-safe SDK releases for `@opsimathically/nmap-extended-sdk`.

- Add changesets for any public API change.
- Run `npm run version-packages` to apply version bumps.
- Run `npm run release` in CI with `NPM_TOKEN` for publish.
